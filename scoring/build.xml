<!-- A "project" describes a set of targets that may be requested
     when Ant is executed.  The "default" attribute defines the
     target which is executed if no specific target is requested,
     and the "basedir" attribute defines the current working directory
     from which Ant executes the requested task.  This is normally
     set to the current working directory.
-->


<project name="My Project" default="compile" basedir=".">

<!-- ===================== Property Definitions =========================== -->

<!--

  Each of the following properties are used in the build script.
  Values for these properties are set by the first place they are
  defined, from the following list:
  * Definitions on the "ant" command line (ant -Dcatalina.home=xyz compile)
  * Definitions from a "build.properties" file in the top level
    source directory
  * Definitions from a "build.properties" file in the developer's
    home directory
  * Default definitions in this build.xml file

  You will note below that property values can be composed based on the
  contents of previously defined properties.  This is a powerful technique
  that helps you minimize the number of changes required when your development
  environment is modified.  Note that property composition is allowed within
  "build.properties" files as well as in the "build.xml" script.

-->
  <property file="build.properties" />



<!-- ==================== File and Directory Names ======================== -->

<!--

  These properties generally define file and directory names (or paths) that
  affect where the build process stores its outputs.

  app.name             Base name of this application, used to
                       construct filenames and directories.
                       Defaults to "myapp".

  app.version          Version identifier for this application.

  build.home           The directory into which the "prepare" and
                       "compile" targets will generate their output.
                       Defaults to "build".

  catalina.home        The directory in which you have installed
                       a binary distribution of Tomcat 4.  This will
                       be used by the "deploy" target.

  deploy.home          The name of the directory into which the
                       deployment hierarchy will be created, and into
                       which the build directory will be copied.
                       Defaults to "${catalina.home}/webapps/${app.name}".

  dist.home            The name of the base directory in which
                       distribution files are created.
                       Defaults to "dist".

  web.home              The directory that contains the web files, defaults to "web"

  lib.home              The directory that contains the libraries, defaults to "lib"

  src.home              The directory that contains the source, defaults to "src"

  tmp.home              Temporary directory for compiling JSP files, defaults to "tmp"

  web.config            What file to use as web.xml.  Default is web-default.xml

  dist.web.config       What file to use as web.xml when building a
                        distribution.  Default is web-default.xml

-->

<property name="app.name"      value="fll-sw"/>
<property name="app.version"   value="2.4"/>
<property name="build.home"    value="build"/>
<property name="catalina.home" value="foo"/>
<property name="deploy.home"   value="${catalina.home}/webapps/${app.name}"/>
<property name="dist.home"     value="dist"/>
<property name="web.home"       value="web" />
<property name="lib.home"       value="lib" />
<property name="src.home"       value="src" />
<property name="tmp.home"       value="tmp" />
<property name="web.config" value="web-default.xml" />
<property name="dist.web.config" value="web-default.xml" />

<!--  ==================== Compilation Control Options ==================== -->

<!--

  These properties control option settings on the Javac compiler when it
  is invoked using the <javac> task.

  compile.debug        Should compilation include the debug option?

  compile.deprecation  Should compilation include the deprecation option?

  compile.optimize     Should compilation include the optimize option?

-->

<property name="compile.debug"       value="true"/>
<property name="compile.deprecation" value="true"/>
<property name="compile.optimize"    value="false"/>



<!-- ==================== External Dependencies =========================== -->


<!--

  Use property values to define the locations of external JAR files on which
  your application will depend.  In general, these values will be used for
  two purposes:
  * Inclusion on the classpath that is passed to the Javac compiler
  * Being copied into the "/WEB-INF/lib" directory during execution
    of the "deploy" target.

  Because we will automatically include all of the Java classes that Tomcat 4
  exposes to web applications, we will not need to explicitly list any of those
  dependencies.  You only need to worry about external dependencies for JAR
  files that you are going to include inside your "/WEB-INF/lib" directory.

-->

<!-- Dummy external dependency -->
<!--
  <property name="foo.jar"
           value="/path/to/foo.jar"/>
-->


<!-- ==================== Compilation Classpath =========================== -->

<!--

  Rather than relying on the CLASSPATH environment variable, Ant includes
  features that makes it easy to dynamically construct the classpath you
  need for each compilation.  The example below constructs the compile
  classpath to include the servlet.jar file, as well as the other components
  that Tomcat makes available to web applications automatically, plus anything
  that you explicitly added.

-->

<path id="application.classpath">
  <pathelement location="${lib.home}/JonsInfra-0.2.jar"/>
  <pathelement location="${lib.home}/junit-3.8.1.jar"/>
  <pathelement location="${lib.home}/log4j-1.2.8.jar"/>
  <pathelement location="${lib.home}/mysql-connector-java-3.0.8-stable-bin.jar"/>
  <pathelement location="${lib.home}/common-lib/xercesImpl.jar"/>
  <pathelement location="${lib.home}/common-lib/xmlParserAPIs.jar"/>
</path>

<path id="compile.classpath">
  <!-- Include all JAR files that will be included in /WEB-INF/lib -->
  <path refid="application.classpath" />

  <pathelement location="${lib.home}/commons-lang-2.0.jar" />
  <pathelement location="${lib.home}/taglibs-string.jar" />
  <pathelement location="${lib.home}/uptag.jar"/>

  <!-- JSTL libraries -->
  <pathelement location="${lib.home}/dom.jar" />
  <pathelement location="${lib.home}/jaxen-full.jar" />
  <pathelement location="${lib.home}/jaxp-api.jar" />
  <pathelement location="${lib.home}/jstl.jar" />
  <pathelement location="${lib.home}/sax.jar" />
  <pathelement location="${lib.home}/saxpath.jar" />
  <pathelement location="${lib.home}/standard.jar" />
  <pathelement location="${lib.home}/xalan.jar" />

  <!-- Include all elements that Tomcat exposes to applications -->
  <pathelement location="${catalina.home}/common/classes"/>
  <fileset dir="${catalina.home}/common/endorsed">
    <include name="*.jar"/>
  </fileset>
  <fileset dir="${catalina.home}/common/lib">
    <include name="*.jar"/>
  </fileset>
  <pathelement location="${catalina.home}/shared/classes"/>
  <fileset dir="${catalina.home}/shared/lib">
    <include name="*.jar"/>
  </fileset>

</path>


<target name="init">
  <tstamp />

  <uptodate property="jsp.notRequired"
            targetfile="${tmp.home}/.tstamp"
            >
    <srcfiles dir="${basedir}/${web.home}" includes="**" />
  </uptodate>
</target>


<!-- ==================== Clean Target ==================================== -->

<!--

  The "clean" target deletes any previous "build" and "dist" directory,
  so that you can be ensured the application can be built from scratch.

-->

<target name="clean"
   description="Delete old build and dist directories">
  <delete dir="${build.home}"/>
  <delete dir="${dist.home}"/>
  <delete dir="${tmp.home}" />
</target>



<!-- ==================== Compile Target ================================== -->

<!--

  The "compile" target transforms source files (from your "src" directory)
  into object files in the appropriate location in the build directory.
  This example assumes that you will be including your classes in an
  unpacked directory hierarchy under "/WEB-INF/classes".

-->
<target name="compile" depends="prepare,compile.classes,compile.jsp"
   description="Compile Java sources">
</target>

<target name="compile.classes" depends="prepare,checkstyle"
   description="Compile Java sources">

  <!-- Compile Java classes as necessary -->
  <mkdir    dir="${build.home}/WEB-INF/classes"/>
  <javac srcdir="src"
            destdir="${build.home}/WEB-INF/classes"
             debug="${compile.debug}"
       deprecation="${compile.deprecation}"
          optimize="${compile.optimize}">
  <classpath refid="compile.classpath"/>
  </javac>
  
  <!-- Copy associated resource files -->
  <copy  todir="${build.home}/WEB-INF/classes">
    <fileset dir="src">
      <exclude name="**/*.java" />
    </fileset>
  </copy>

</target>

<!-- not perfect, but will work -->
<target name="compile.jsp" depends="compile.classes"
    description="Compile JSP sources"
    unless="jsp.notRequired"
    >
    
    <mkdir    dir="${tmp.home}" />
<!--    <jspc srcdir="${web.home}"
          destdir="${tmp.home}">
      <include name="**/*.jsp" />
      <classpath>
        <pathelement location="${build.home}/WEB-INF/classes" />
        <path refid="compile.classpath" />
      </classpath>
    </jspc> -->

    <java classname="org.apache.jasper.JspC"
	  fork="yes"
	  failonerror="true"
	  taskname="jspc"
	  dir="${basedir}"
          >
      <classpath>
        <pathelement location="${build.home}/WEB-INF/classes" />
        <path refid="compile.classpath" />
      </classpath>
      <arg line="-d ${tmp.home}" />
      <arg line="-die9" />
      <arg line="-webapp ${build.home}" />
    </java>

    <javac srcdir="${tmp.home}"
           destdir="${tmp.home}"
           debug="${compile.debug}"
           deprecation="${compile.deprecation}"
           optimize="${compile.optimize}">
    <classpath>
      <pathelement location="${build.home}/WEB-INF/classes" />
      <path refid="compile.classpath" />
    </classpath>
    </javac>

</target>  


<!-- ==================== Deploy Target =================================== -->

<!--

  The "deploy" target copies the contents of the build directory into a
  location required by our servlet container, and picks up any external
  dependencies along the way.  AFter restarting the servlet container, you
  can now test your web application.

-->

<target name="deploy" depends="compile"
   description="Deploy application to servlet container">

  <!-- first clean out the old files -->
  <delete>
    <fileset dir="${deploy.home}">
      <include name="**" />
    </fileset>
  </delete>

  <!-- Copy the contents of the build directory -->
  <mkdir     dir="${deploy.home}"/>
  <copy    todir="${deploy.home}">
    <fileset dir="${build.home}"/>
  </copy>

</target>



<!-- ==================== Dist Target ===================================== -->


<!--

  The "dist" target creates a binary distribution of your application
  in a directory structure ready to be archived in a tar.gz or zip file.
  Note that this target depends on two others:
  * "deploy" so that the entire web application (including external
    dependencies) will have been assembled
  * "javadoc" so that the application Javadocs will have been created

-->

<target name="dist" depends="compile,javadoc"
        description="Create distribution">

  <!-- copy docs -->
  <mkdir dir="${dist.home}/docs" />
  <copy todir="${dist.home}/docs">
    <fileset dir="${basedir}/docs" />
  </copy>

  <!-- get the correct web.xml file -->
  <copy file="${web.home}/WEB-INF/${dist.web.config}"
        tofile="${build.home}/WEB-INF/web.xml"
        overwrite="true"
        />

  <!-- Create web application zip file -->
  <zip zipfile="${dist.home}/web.zip">
    <zipfileset dir="${build.home}" prefix="fll-sw" />
  </zip>
  
  
  <zip zipfile="${dist.home}/common-lib.zip">
    <zipfileset dir="${lib.home}/common-lib"/>
  </zip>
  
  
  <!-- stuff for subjective scoring app -->
  <mkdir dir="${tmp.home}/subjective" />
  <jar destfile="${tmp.home}/subjective/subjective.jar"
       basedir="${build.home}/WEB-INF/classes" />
  <copy todir="${tmp.home}/subjective">
    <fileset dir="${lib.home}" />
  </copy>

  <path id="subjective.dist.classpath">
    <path refid="application.classpath" />
    <pathelement location="${lib.home}/subjective.jar" />
  </path>
  <pathconvert targetos="windows"
               property="subjective.windows.classpath"
               refid="subjective.dist.classpath">
    <map from="${basedir}/${lib.home}" to="." />
  </pathconvert>
  <pathconvert targetos="unix"
               property="subjective.unix.classpath"
               refid="subjective.dist.classpath">
    <map from="${basedir}/${lib.home}" to="." />
  </pathconvert>


  <echo file="${tmp.home}/subjective/subjective.bat">%JAVA_HOME%\bin\java -classpath "${subjective.windows.classpath}" fll.gui.SubjectiveFrame</echo>
  
  <echo file="${tmp.home}/subjective/subjective.sh">java -classpath "${subjective.unix.classpath}" fll.gui.SubjectiveFrame</echo>
  
  <zip zipfile="${dist.home}/subjective.zip">
    <zipfileset prefix="subjective" dir="${tmp.home}/subjective" />
  </zip>
  
  <zip zipfile="${dist.home}/src.zip">
    <zipfileset dir="${basedir}">
      <include name="${web.home}/**" />
      <include name="${src.home}/**" />
      <include name="docs/**" />
      <include name="prj.el" />
      <include name="build.xml" />
      <include name="ChangeLog" />
    </zipfileset>
  </zip>
  
  <zip zipfile="${app.name}-${app.version}.zip">
    <zipfileset dir="${dist.home}" prefix="fll" />
  </zip>
  
  <delete dir="${tmp.home}" />
  
  <echo message="The distribution file is ${app.name}.zip" />
 
</target>



<!-- ==================== Javadoc Target ================================== -->

<!--

  The "javadoc" target creates Javadoc API documentation for the Java
  classes included in your application.  Normally, this is only required
  when preparing a distribution release, but is available as a separate
  target in case the developer wants to create Javadocs independently.

-->

<target name="javadoc"
   description="Create Javadoc API documentation">

  <mkdir dir="${dist.home}/api"/>
  <javadoc sourcepath="src"
           windowtitle="${app.name}"
           destdir="${dist.home}/api"
           packagenames="fll.*"
           use="true"
           splitindex="true"
           additionalparam="-tag beaninfo:a:BeanInfo -tag valid:a:Validity -tag feasible:a:Feasibility -tag test:a:Tests -tag pre:cm:PreConditions -tag post:cm:PostConditions -tag invariant:tc:Invariants -breakiterator"
           >
    <classpath>
      <path refid="compile.classpath" />
      <pathelement location="${build.home}/src" />
    </classpath>
  
  </javadoc>

</target>



<!-- ==================== Prepare Target ================================== -->

<!--

  The "prepare" target is used to create the "build" destination directory,
  and copy the static contents of your web application to it.  If you need
  to copy static files from external dependencies, you can customize the
  contents of this task.

  Normally, this task is executed indirectly when needed.

-->

<target name="prepare" depends="init">

  <!-- Create build directory and copy static content -->
  <mkdir  dir="${build.home}"/>
  <copy todir="${build.home}">
    <fileset dir="${web.home}">
      <exclude name="**/*.java" />
      <exclude name="**/common-lib" />
    </fileset>
  </copy>
  
  <!-- Copy static files from external dependencies as needed -->
  <copy todir="${build.home}/WEB-INF/classes">
    <fileset dir="${src.home}">
      <exclude name="**/*.java" />
    </fileset>
  </copy>
  <copy todir="${build.home}/WEB-INF/lib">
    <fileset dir="${lib.home}">
      <include name="**/*.jar" />
      <exclude name="**/common-lib/**" />
    </fileset>
  </copy>

  <!-- get the correct web.xml file -->
  <copy file="${web.home}/WEB-INF/${web.config}"
        tofile="${build.home}/WEB-INF/web.xml"
        overwrite="true"
        />

</target>

  <!-- ===================== Checkstyle =================== -->
  <taskdef resource="checkstyletask.properties">
    <classpath>
      <pathelement location="lib/ant/antlr.jar" />
      <pathelement location="lib/ant/checkstyle-3.3.jar" />
      <pathelement location="lib/ant/commons-beanutils.jar" />
      <pathelement location="lib/ant/commons-cli-1.0.jar" />
      <pathelement location="lib/ant/commons-collections.jar" />
      <pathelement location="lib/ant/commons-logging.jar" />
      <pathelement location="lib/ant/jakarta-regexp-1.3.jar" />
    </classpath>
  </taskdef>

  <target name="checkstyle"
          description="Check for code convention violations"
          >
    <mkdir dir="${build.home}/classes" />

    <checkstyle config="checkstyle_checks.xml">
      <fileset dir="${basedir}/src">
        <include name="**/*.java" />
      </fileset>
    </checkstyle>
  </target>

  <target name="checkstyle.nightly"
          description="Check for code convention violations (more aggressive)"
          >
    <mkdir dir="${build.home}/classes" />

    <checkstyle config="checkstyle_nightly_checks.xml"
                failOnViolation="false"
                >
      <fileset dir="${basedir}/src">
        <include name="**/*.java" />
      </fileset>
    </checkstyle>
  </target>

</project>
